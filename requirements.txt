pandas>=2.0
numpy>=1.24
networkx>=3.0
scikit-learn>=1.3
matplotlib>=3.7
openpyxl>=3.1

# Not required by anything in this module today. If you swap gnn_model.py
# for a real torch_geometric.nn.GCNConv encoder (see gnn_model.py docstring),
# add:
# torch>=2.0
# torch-geometric>=2.4

# Only needed if you have genuinely legacy binary .xls files (pre-2007
# format) - openpyxl cannot read these. Most files with an .xls extension
# in the sample dataset turned out to actually be xlsx-format internally
# and don't need this. See README "Known gaps".
# xlrd>=2.0
